<!-- Scaffolding generated by Casein v5.1.1.1 -->

<h2><%= @casein_page_title %></h2>

<%= form_for @coordenada, :url => casein_coordenada_path(@coordenada) do |f| %>

	<%= render :partial => 'casein/coordenadas/form', :locals => { :f => f } %>

	<p class="submits">
	  	<%= link_to 'Cancelar', casein_coordenadas_path, :class => 'btn btn-mini btn-danger' %>
		<%= f.submit "Salvar", :class => "btn btn-mini btn-primary" %>
	</p>

<% end %>

<%= content_for :sidebar do %>
	<li><%= link_to "#{casein_show_icon "th-list"}Volta à Listagem".html_safe, casein_coordenadas_path %></li>
<% end %>
