<!-- Scaffolding generated by Casein v5.1.1.1 -->

<table class="table table-striped table-hover table-condensed">
	<thead>
		<tr>
			<th><%= casein_sort_link "Cliente", :cliente_id %></th>
			<th><%= casein_sort_link "Data da Proposta", :data_proposta %></th>
			<th><%= casein_sort_link "Funcionário", :casein_admin_user_id %></th>
			<th>&nbsp;</th>
		</tr>
	</thead>
	
	<% orcamentos.each do |orcamento| %>
		<% @usuario = Casein::AdminUser.find(orcamento.casein_admin_user_id) %>
		<tr>
			<td><%= casein_table_cell_link orcamento.cliente.pessoa.try(:nome), casein_orcamento_path(orcamento) %></td>
			<td><%= casein_table_cell_link orcamento.data_proposta, casein_orcamento_path(orcamento) %></td>
			<td><%= @usuario.name %></td>
			<td class="delete">
				<%= link_to(casein_show_row_icon("trash"), casein_orcamento_path(orcamento), :method => :delete, :data => { :confirm => "Realmente deseja remover esse registro?" }) %>
			</td>
		</tr>
	<% end %>
</table>