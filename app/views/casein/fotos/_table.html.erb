<!-- Scaffolding generated by Casein v5.1.1.1 -->

<table class="table table-striped table-hover table-condensed datatable" cellspacing="0" width="100%"">
	<thead>
		<tr>
			<th>Foto</th>
			<th>Cliente</th>
			<th data-orderable="false">&nbsp;</th>
		</tr>
	</thead>
	
	<% fotos.each do |foto| %>
		<% unless foto.poco.present? %>
			<tr>
				<td><%= casein_table_cell_link image_tag(foto.foto.url(:thumb), width: 20), foto.foto.url(:grande), target: :_blank %></td>
				<td><%= casein_table_cell_link foto.nome, casein_foto_path(foto) %></td>
				<td>
					<%= link_to(casein_show_row_icon("zoom-in"), casein_foto_path(foto), class: "icon-table") if can? :show, Foto %>
						<%= link_to(casein_show_row_icon("edit"), edit_casein_foto_path(foto), class: "icon-table") if can? :edit, Foto %>
						<%= link_to(casein_show_row_icon("trash"), casein_foto_path(foto), class: "icon-table", :method => :delete, :data => { :confirm => "Você tem certeza que deseja apagar este registro?" }) if can? :destroy, Foto %>
				</td>
			</tr>
		<% end %>
	<% end %>
</table>