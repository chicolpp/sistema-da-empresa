<h2>
	<%= @casein_page_title %>
	<%= link_to "#{casein_show_icon "plus-sign"}Novo Cliente".html_safe, new_casein_pessoa_path, class: "pull-right" if can? :new, Pessoa %>
</h2>

<%= render :partial => 'casein/clientes/table' %>