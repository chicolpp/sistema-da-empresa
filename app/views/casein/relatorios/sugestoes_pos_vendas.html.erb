<!-- Scaffolding generated by Casein v5.1.0.0 -->

<div id="printableArea">
  <h2>
    Relatório de Sugestões do Pós Venda
  </h2>

  <table class="table table-striped table-bordered datatable" cellspacing="0" width="100%">
    <thead>
      <tr>
        <th>Sugestão</th>
        <th>Criado em</th>
      </tr>
    </thead>

    <tbody>
      <% @sugestoes.each do |sugestao| %>
        <tr>
          <td><%= link_to(sugestao.descricao, sugestao.tipo == '1' ? casein_assistencia_posvenda_path(sugestao.id) : casein_perfuracao_posvenda_path(sugestao)) %></td>
          <td><%= l(sugestao.created_at) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>